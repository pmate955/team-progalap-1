const name = 'Géza'; // Konstans, ami szöveget (string) tárol
const age = 24; // Konstans, ami számot tárol
console.log(age);
const a = 10;
const b = 5 + 5;
const c = a + b;
const d = (2 + a) + a * 2;
const e = a / 2; // Sima osztás
console.log('A =', a, 'B =', b, 'C =', c);
const f = 12 % 2; // Maradékos osztás
console.log(f);
const g = 2 ** 10; // Hatványozás, Math.pow(2, 10)
const h = Math.pow(2, 10);
const i = Math.sqrt(81);
const j = Math.round(Math.random() * 10) + 10;
console.log(g, j);
